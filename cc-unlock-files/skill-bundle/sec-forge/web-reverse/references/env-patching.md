# Env Patching Bridge

The formal env patching contract now lives in `docs/reference/env-patching.md`.
Bridge inventory lives in `docs/reference/bridge-index.md`.

Use this bridge only as a compatibility pointer.
Do not maintain separate normative content here.
