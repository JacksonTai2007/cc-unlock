# MCP Task Template

- 先读 `task.json`
- 再读 `state/route-state.json`
- 再把 `state/route-plan.md`、`state/clues.md`、`state/progress.md` 当作派生视图补充阅读
- 先以 `route-state.json` 恢复活跃线路、切入点与检查点
- 若 `execution.status=ready-to-continue`，继续执行 `nextExecutableAction`，不要只做状态汇报
