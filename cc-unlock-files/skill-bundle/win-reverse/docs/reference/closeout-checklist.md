<!-- publish: framework -->
# Closeout Checklist

1. `task.json`、`report.md`、`run/fixtures.json` 存在且可读取
2. `report.md` 至少包含“当前阶段”“自动续跑决策”“下一步”
3. `route-state.json` 存在，且与 `route-plan / clues / progress` 同步
4. 命中专题后的必需产物已落盘
